[pytest]
addopts = -ra --strict-markers --html=playwright-report/index.html --self-contained-html
asyncio_mode = auto
testpaths = tests
markers =
    api: API-level checks
    e2e: browser-based end-to-end checks
    security: security-focused checks
