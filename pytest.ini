[pytest]
addopts = -ra --strict-markers
asyncio_mode = auto
log_cli = true
log_cli_level = INFO
testpaths = tests
markers =
    api: API-level checks
    e2e: browser-based end-to-end checks
    security: security-focused checks
